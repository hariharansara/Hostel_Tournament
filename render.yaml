services:
  - type: web
    name: hostel-tournament
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: MONGO_URI
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
